{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimum permissions for the Pathlight desktop window.",
  "windows": ["main"],
  "permissions": ["core:default", "shell:allow-spawn"]
}
